# Session Status — 2026-05-03 (afternoon work while user away)

Saved here for the user to read on return. Mirrors what I'll send as a message.

## TL;DR

1. **CRITICAL BUG FIXED** (15-second find, 1-line fix): `submissions/cd_lns_sa_hybrid/placer.py` had `parents[3]` (correct for `experiments/E48/code/` depth, wrong at `submissions/cd_lns_sa_hybrid/`). The promoted champion file did not import on a fresh `evaluate` invocation. Fixed to `parents[2]`. **Without this fix, partcl evaluation of our submission would have failed with `FileNotFoundError`.** All other submissions correctly use `parents[2]`.

2. **Hardware-regression risk: characterized.** Parsed existing E41 / E25 `--all` logs and found:
   - **CD phase hits hard cap on 5/17 benches in E41 lane** (ibm10/12/14/16/17 — the largest). 2/17 in E25 lane.
   - **K-joint always runs to 600s budget cap** (by design — no plateau detection).
   - LNS / SA-v2 already plateau under budget.
   - Implication: the 5 cap-hitters are hardware-fragile; on slower per-core hardware they would converge less. Aligns with the leaderboard's "Incremental CD+LNS" reportedly dropping from 1.1172 to ~12th place on partcl.

3. **Multi-day research delivered.** Spawned a research agent in background (completed 11min 648s); produced `docs/research_principles_for_walls.md` (8612 words). Three top-EV recommendations:
   - **Rank 1 (E67): K=50 Hungarian re-pack** — non-local feasibility-respecting move, 2-3 days dev, hardware-robust if capped on moves. Direct attack on local-move plateau.
   - **Rank 2 (E68): Constrained NEB on the feasible manifold** — chemistry/materials gold standard for finding new minima (Henkelman 2000 + Raja 2025), 5-7 days dev, highest theoretical EV.
   - **Rank 3 (E63 V4): Spectral init with obstacle-aware Tetris legalizer** — cheap unblock for existing E63 lane, 1-2 days. **Implemented today.**
   - Plus a hardware-mitigation refactor proposal (§4): change CD/LNS/SA/K-joint termination from wall-bounded primary to plateau-bounded primary, ~2-3 hour refactor.
   - Surprising finding: **Xue et al. 2024 NeurIPS independently discovered the E48 hybrid pattern** (refining a pre-existing placement is structurally easier than placing from scratch). Two parallel research traditions arrived at the same insight — citable as contemporaneous validation, not competing prior art.

4. **E63 V4 unblocked.** The blocker wasn't fixed-macro overlaps (manifest's diagnosis was wrong — all benches have 0 fixed macros). The actual bug was **float32 precision artifact** on edge-to-edge placements (`cur_x += w` exactly produces 1e-7 "overlaps" of zero area that compute_overlap_metrics counts). Fix: EPS=1e-3 micron gap between adjacent macros. Plus safe abort-on-vertical-overflow. **V4 works on 11/17 IBM benches** (ibm01/04/06/07/08/09/14/15/16/17/18 = OK; ibm02/03/10/11/12/13 = vertical overflow).

5. **Two experiments running in background:**
   - `e66_hardware_probe_t1_ibm01`: E48 hybrid on ibm01 with all OMP/MKL/OPENBLAS/VECLIB/NUMEXPR threads = 1. ETA 30 min from now.
   - `e63_v4_spectral_ibm04`: E63 V4 single-bench on ibm04. ETA 30-50 min.

## Detailed findings

### 1. Path bug in promoted champion

```python
# Before (broken):
_ROOT = Path(__file__).resolve().parents[3]  # /Users/brendan/Developer
_E25_PATH = _ROOT / "submissions" / "cd_lns_sa" / "placer.py"
# → /Users/brendan/Developer/submissions/cd_lns_sa/placer.py (doesn't exist)

# After (fixed):
_ROOT = Path(__file__).resolve().parents[2]  # macro-place-challenge-2026
_E25_PATH = _ROOT / "submissions" / "cd_lns_sa" / "placer.py"
# → /Users/brendan/Developer/macro-place-challenge-2026/submissions/cd_lns_sa/placer.py (correct)
```

Import test confirmed.

The original `experiments/E48_hybrid_e25_e41/code/cd_lns_sa_hybrid.py` works correctly because `parents[3]` from `experiments/E48_hybrid_e25_e41/code/` resolves to project root. When the file was copied to `submissions/cd_lns_sa_hybrid/placer.py` for ADR-011 promotion, the path level wasn't updated. **The previous --all run that produced 1.08151 used the experiments file**, so the score is valid; only the submission-ready file was broken.

### 2. Phase exit profile (existing E41 / E25 --all logs)

Mined `experiments/E41_dpo_kjoint/run_all.log` and `experiments/E25_lns_sa_compose/run_all.log`. Per-phase termination reasons across 17 IBM benches:

**E41 lane (DPO + CD + LNS + SA + K-joint):**
- CD: 12 plateau, **5 cap** (2400s wall hit).
- LNS: 17 plateau (saturate 16-397s out of 600s budget).
- SA-v2: 14 saturated (best == CD output, +0.00000 lift).
- K-joint: 14 cap, 3 plateau.

**E25 lane (SDF + CD + LNS + SA):**
- CD: 15 plateau, **2 cap**.
- LNS, SA: same as E41 (plateau / saturated).

**Cap-hitters (E41 CD)** — by proxy match against per-bench table:
- ibm10 (1.0274 → 1.0096 final), ibm12 (1.2077 → 1.2064), ibm14 (1.1970 → 1.1995), ibm16 (1.1476 → 1.1440), ibm17 (1.3410 → 1.3324).

These are the 5 largest IBM benches by macro count. On slower per-core hardware, fewer CD sweeps fit in 2400s, so they converge less.

**K-joint is budget-bound by design** (no plateau detection). This contributes to hardware sensitivity less catastrophically than CD because K-joint commits incrementally (each commit is independent), but on slower hardware fewer K-tuples are tried.

### 3. E63 V4 status

| Bench | n_hard | density | V4 result | Wall |
|---|---:|---:|---|---:|
| ibm01 | 246 | 0.428 | OK (0 residuals) | 0.2s |
| ibm02 | 459 | (?) | FAIL (153 residuals) | 1.6s |
| ibm03 | 463 | (?) | FAIL (155 residuals) | 1.5s |
| ibm04 | 295 | 0.420 | OK | 0.4s |
| ibm06 | 178 | (?) | OK | 0.4s |
| ibm07 | 291 | (?) | OK | 0.6s |
| ibm08 | 301 | (?) | OK | 0.6s |
| ibm09 | 253 | 0.398 | OK | 0.4s |
| ibm10 | 786 | (?) | FAIL (219 residuals) | 4.4s |
| ibm11 | 373 | (?) | FAIL (155 residuals) | 1.6s |
| ibm12 | 651 | (?) | FAIL (438 residuals) | 4.0s |
| ibm13 | 424 | 0.362 | FAIL (210 residuals) | 3.6s |
| ibm14 | 614 | 0.197 | OK | 1.3s |
| ibm15 | 393 | (?) | OK | 0.6s |
| ibm16 | 458 | (?) | OK | 0.7s |
| ibm17 | 760 | 0.172 | OK | 1.7s |
| ibm18 | 285 | 0.087 | OK | 0.4s |

Failure mode on ibm02/03/10/11/12/13: density × max-macro-height products that force vertical overflow. V4's "drop by max(row_max_h, h)" leaves no room for remainder macros.

**V5 Tetris-with-row-spanning** was implemented (Spindler-Schlichtmann 2008 Abacus-style with pre-allocated uniform row tracks + tall-macro multi-row spanning) but produced WORSE results (only 4/17 working). Reverted; V4 used as default. V5 left in code as scaffolding.

The 11/17 working set is enough to test E63 as a hybrid lane. Per-bench best-of will fall back to E48 on the 6 failing benches.

### 4. Research doc highlights

`docs/research_principles_for_walls.md` covers literature (2023-2026) on:
- Disconnected feasible sets (bilevel programming, constrained MOO)
- Constrained NEB / manifold optimization (Raja et al. 2025 OM-action; null-space gradient flows 2024)
- Score-based / diffusion sampling for combinatorial problems (DIFUSCO 2023, DiffUCO 2024, Lai 2024 chip placement)
- Hierarchical / multilevel placement (hMETIS, Hier-RTLMP 2024, GOALPlace 2024)
- BP / tensor networks on factor graphs
- GNN / RL placement (Mirhoseini 2021, Xue 2024 RL-as-regulator)
- Hungarian / assignment / OT (Sinkhorn, Hier-Refinement OT 2025)
- Hardware-invariant numerical optimization (work-bounded vs wall-bounded)

The 7 ranked candidate mechanisms with manifest skeletons cover the 4 walls + hardware. The recommended deadline sequence:
- Days 1-2 (today): E63 V4 — done (partial 11/17, but mechanism unblocked)
- Days 3-5: E67 K=50 Hungarian re-pack — recommended next experiment
- Days 6-9: E64 LP-bounded beam K-joint — already scaffolded
- Days 10-15: E68 constrained NEB if any of above show saddle structure
- Days 16-18: verify + submit

The hardware mitigation (§4.5) recommends a 2-3 hour refactor of E48's CD/LNS/SA/K-joint termination to be plateau-bounded primary, wall-bounded soft-secondary.

## What's running / completed

### E66 Hardware Probe ibm01 thread-limit — COMPLETED 2026-05-03 14:58 EDT

| Lane | Proxy | Wall |
|---|---:|---:|
| E25 (SDF init + CD + LNS + SA-v2) | **0.89004** | 1149 s |
| E41 (DPO init + CD + LNS + SA-v2 + K-joint) | 0.91207 | 1699 s |
| **E48 = min(E25, E41)** | **0.89004** | **2848 s total** |

vs historic E48 ibm01 = 0.89234. **Probe BEAT historic by −0.26 %** under thread-limit (all OMP/MKL/OPENBLAS/VECLIB/NUMEXPR=1).

**Interpretation:** thread-limit on M3 Max does NOT degrade ibm01 score. Single-threaded execution actually performed marginally better — likely from reduced thread-contention in BLAS / NumPy. **However, ibm01 is not a CD cap-hitter** (CD plateau-exits well before 2400s). The cap-hitters (ibm10/12/14/16/17) remain the regression-risk benches; thread-limit's effect on them is what we should test next.

**Cap-hitter probe queued:** ibm10 thread-limit probe launched at 14:58 EDT (background id `bm0tj73r1`); ETA ~3 hr (cap-hitter under thread-limit). Will report when complete.

### E63 V4 spectral on ibm04 — COMPLETED 2026-05-03 15:04 EDT

| Phase | Proxy | Δ |
|---|---:|---:|
| Spectral init | 1.889 | (start) |
| After CD (plateau-exit at 22 sweeps) | 1.275 | −0.614 |
| After LNS (plateau-exit) | 1.209 | −0.066 |
| After SA-v2 | 1.194 | −0.015 |
| After K-joint (2 passes, 71 commits) | 1.183 | −0.011 |
| **Final validated proxy** | **1.1887** | **(total −0.700)** |

**vs E48 ibm04 = 0.98506. That's +20.7 % WORSE.** Total wall 2300s.

**Verdict: E63 spectral lane FALSIFIED on ibm04.** The basin landed by CD on legalized spectral coords is structurally ~20 % above the SDF/DPO basins. Pattern matches E62 WillSeed: **standalone init quality predicts polished basin quality** (spectral 1.89 standalone → 1.19 polished; WillSeed 1.53 → 0.93; SDF 1.50 → 0.99; DPO 1.38 → 0.95).

**Why it failed:** the spectral coordinate system encodes netlist topology, but post-legalization (row-aligned slots) destroys the topology signal. CD-LNS-SA-K-joint then descends from a structurally inferior starting state. E63 V4 unblocked the legalizer (rank-3 research recommendation succeeded mechanically), but the underlying hypothesis ("spectral lane is a useful orthogonal basin") does not pan out.

**Recommended close:** mark E63 line as falsified post-V4. Single-bench evidence is strong; verifying on 1-2 more benches is optional but probably redundant given E62 already established the pattern. **Do not pursue spectral lane further.**

### E66 ibm10 thread-limit probe — COMPLETED 2026-05-03 16:48 EDT

| Lane | Proxy | Notable |
|---|---:|---|
| E25 (SDF init pipeline) | 1.04711 | (lost to E41) |
| E41 (DPO init + K-joint) | **1.00408** | **CD plateau-exit at 1829s, NOT cap** |
| **E48 = min(E25, E41)** | **1.00408** | Total wall 6588 s |

vs historic E48 ibm10 = 1.00961. **Probe BEAT historic by −0.55 %.**

**Single most important finding of this session:** The historic `--jobs 4` E41 ibm10 run hit CD's 2400s hard cap at sweep 11, proxy 1.02741. Under thread-limit single-job, **CD plateau-exited at 1829s** at virtually the same proxy (1.02719). Same quality reached, but with 570s of budget headroom.

This means the "5 cap-hitters on E41 lane" finding (ibm10/12/14/16/17 hitting CD's 2400s wall) was **largely a `--jobs 4` parallel-contention artifact, not an algorithmic-quality limit**. Under serial execution each CD sweep takes ~160s vs ~218s parallel — fewer contention misses → plateau detection fires before cap.

**Implication for partcl risk:** if partcl runs each bench serially (which is the natural per-bench-1-hr-cap model), our pipeline will plateau-exit cleanly with budget margin. The hardware-regression scenario where we run out of budget mid-CD requires both (a) partcl's per-core clock to be much slower than M3 Max AND (b) the slowdown to be larger than our 30 % budget headroom (1829s vs 2400s on ibm10). Not impossible, but much less likely than the original cap-hitter analysis suggested.

**De-risks the partcl regression substantially.**

## Recommended next steps when you're back

1. **Read the research doc** (`docs/research_principles_for_walls.md`) — particularly §3 ranked plan and §4.5 hardware mitigation.
2. **Decide on rank-1 K=50 Hungarian re-pack** (E67) — 2-3 days dev, highest tractable EV. The simplest description: at any plateau, pick a 50-macro coupled cluster, generate 100 candidate slots, solve Hungarian assignment minimizing proxy delta, commit. This is the mechanism class explicitly called out by E65's manifest as a plausible breakthrough.
3. **Decide on hardware-mitigation refactor** — work-bounded termination as primary. Cheapest defense against partcl regression. ~2-3 hours.
4. **Re-decide on E63**: even at 11/17 working, if spectral basin polishes to comparable proxy as SDF/DPO, could be a 3rd hybrid lane. The single-bench ibm04 result will tell us. If polished spectral basin > 1.10 average, E63 line closes here.
5. **ADR-012 promotion** still pending (3-lane hybrid with E61_v2). Independent of the above.

## Tasks (TaskList)

| ID | Subject | Status |
|---|---|---|
| 1 | Hardware regression risk: simulate partcl hardware | in_progress (probe finishing in ~30 min) |
| 2 | Deep research: principles to break the infeasibility wall | **completed** (`docs/research_principles_for_walls.md`) |
| 3 | E63 spectral legalization fix: row-pack with fixed-region subtraction | **completed** (V4 works on 11/17) |
| 4 | Plateau-vs-budget analysis on E48 components | in_progress (analysis done; probe verifying) |
| 5 | Critical: fix path bug in `submissions/cd_lns_sa_hybrid/placer.py` | **completed** |
| 6 | Run E63 V4 single-bench on ibm04 to measure spectral basin lift | pending (running) |
| 7 | Refactor E48 to work-bounded termination (post-experiments) | pending |

I'll send a follow-up message with final probe + E63 V4 results when they land.
